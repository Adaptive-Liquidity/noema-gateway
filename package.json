{
  "name": "noema-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal authenticated NOEMA instruction gateway (preview only)",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "tsx --test test/**/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.17.1",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
