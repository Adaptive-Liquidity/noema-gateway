interface:
  display_name: "Noema Gateway"
  short_description: "Repo-specific patterns and workflows for noema-gateway"
  default_prompt: "Use the noema-gateway repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true